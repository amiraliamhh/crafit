import React from 'react';

import HeaderStyles from './header.styled';

function Header() {
    return (
        <HeaderStyles>
            header
        </HeaderStyles>
    );
}

export default Header;
